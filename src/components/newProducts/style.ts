import styled from "styled-components";

export const NewSection = styled.section`
  height: 816px;
  padding-top: 9px;

  background-color: var(--color-just-arrived);

  .carousel_moreButton {
    display: none;
  }
`;
